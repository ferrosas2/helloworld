# Core API Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0

# LangChain and AI
# Use compatible ranges within the 0.1.x line. langchain-core and
# langchain-text-splitters are resolved automatically as dependencies of langchain,
# so they are intentionally not pinned here to avoid version conflicts.
langchain>=0.1.16,<0.2
langchain-google-vertexai>=0.1.0,<0.2

# Google Cloud Platform
google-cloud-aiplatform==1.42.1
google-cloud-storage==2.14.0
google-auth==2.27.0

# Data Processing
pandas==2.2.0

# HTTP Client (for health checks)
requests==2.31.0

# Testing (dev dependencies - install separately for CI/CD)
# pytest==8.0.0
# pytest-cov==4.1.0
# pytest-asyncio==0.23.4
# httpx<0.28  # starlette 0.35.x TestClient is incompatible with httpx>=0.28 (removed the `app=` shortcut)

# RAGAS evaluation (dev dependency — install separately)
# pip install ragas==0.1.21 datasets==2.19.0
# ragas==0.1.21
# datasets==2.19.0
